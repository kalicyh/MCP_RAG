# 模型类型（OLLAMA 或 OPENAI）
MODEL_TYPE=OPENAI

# Ollama 配置
OLLAMA_MODEL=phi3
OLLAMA_TEMPERATURE=0

# OpenAI 配置
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0

# 向量嵌入提供商
# 可选：HF（本地 HuggingFace）或 OPENAI（云端 OpenAI Embeddings）
# 选择 OPENAI 将使用 text-embedding-3-large（可通过下方变量自定义）
EMBEDDING_PROVIDER=OPENAI

# 当 EMBEDDING_PROVIDER=OPENAI 时生效
# 默认使用 OpenAI 的高维度模型（3072 维）
OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# 向量库集合基础名称（最终集合名会按提供商/模型自动添加后缀避免维度冲突）
# 例如：default_collection-openai_text-embedding-3-large 或 default_collection-hf_all-MiniLM-L6-v2
COLLECTION_NAME=default_collection
