# Minimal requirements for RAG MCP Server
# Install with: pip install -r requirements-minimal.txt
# Use this for quick testing or when you want minimal dependencies

# Core dependencies only
mcp[cli]==1.0.0
langchain==0.1.0
langchain-community==0.0.20
chromadb==0.4.22
markitdown[all]==0.0.1
sentence-transformers==2.2.2
python-dotenv==1.0.0
httpx>=0.27.0 